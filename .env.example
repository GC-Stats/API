DATABASE_URL=
REDIS_URL=
REDIS_LOCAL_URL=

# Random secret (>= 64 bytes) used to sign the dashboard session cookie
DASHBOARD_COOKIE_SECRET=

APP_ENV=production

# Production only
ALLOWED_ORIGIN=https://api.gc-stats.app

# Optional (defaults shown)
#BIND_ADDR=0.0.0.0:3000
#DATABASE_MAX_CONNECTIONS=10